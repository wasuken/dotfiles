# 困ったときメモ

## zsh

```bash
# homeディレクトリで
$ ln -sf dotfiles/.zshrc ./
# とりあえず作る
$ touch dotfiles/zsh/config.zsh
```

## nvim

```bash
# $home/.config/ディレクトリで
$ ln -sf ~/dotfiles/nvim ./
```

## emacs

```bash
$ ln -sf ~/dotfiles/init.el ~/.emacs.d/init.el
```
